import { type WorkspaceMetadata } from "./workspaces.js";
import { type EvalVerdict } from "./state-machine.js";
import { type AnchorConfig } from "./config.js";
import { type CommandRunner } from "./agent-runner.js";
export type EvaluatorAdapter = "fixture" | "codex" | "pi";
export type EvaluatorReport = {
    adapter: EvaluatorAdapter;
    provider: EvaluatorAdapter;
    verdict: EvalVerdict;
    taskId: string;
    attempt?: number;
    startedAt: string;
    finishedAt: string;
    testsRun: number;
    testsFailed: number;
    feedback: string;
    filesInspected: string[];
    generatorReportPath: string;
    summary: string;
    command?: string;
    argv?: string[];
    exitCode?: number | null;
    stdoutSummary?: string;
    stderrSummary?: string;
};
export type EvaluatorOk = {
    ok: true;
    report: EvaluatorReport;
    reportPath: string;
};
export type EvaluatorError = {
    ok: false;
    code: "UNSUPPORTED_ADAPTER" | "UNKNOWN_PROVIDER" | "UNSUPPORTED_PROVIDER_ROLE" | "INVALID_VERDICT" | "WORKSPACE_UNAVAILABLE" | "GENERATOR_REPORT_NOT_FOUND" | "CODEX_CLI_UNAVAILABLE" | "CODEX_COMMAND_FAILED" | "CODEX_NO_VERDICT" | "PI_CLI_UNAVAILABLE" | "PI_COMMAND_FAILED" | "PI_NO_VERDICT";
    message: string;
    detail?: string;
    report?: EvaluatorReport;
    reportPath?: string;
};
export type RunEvaluatorInput = {
    taskId: string;
    artifactsDir: string;
    workspace: WorkspaceMetadata;
    contract: string;
    contractPath?: string;
    adapter: string;
    verdict?: string;
    attempt?: number;
    generatorReportPath?: string;
    reportPath?: string;
    config?: AnchorConfig;
    allowNetwork?: boolean;
    retryFailTimes?: number;
};
export declare function runEvaluator(input: RunEvaluatorInput, runner?: CommandRunner): Promise<EvaluatorOk | EvaluatorError>;
export declare function validateEvaluatorProvider(providerId: string, runner?: CommandRunner): {
    ok: true;
} | EvaluatorError;
export declare function runFixtureEvaluator(input: RunEvaluatorInput): Promise<EvaluatorOk | EvaluatorError>;
export declare function writeEvaluatorReport(artifactsDir: string, taskId: string, report: EvaluatorReport, targetPath?: string): Promise<string>;
export declare function evaluatorReportPath(artifactsDir: string, taskId: string): string;
export declare function evaluatorAttemptReportPath(artifactsDir: string, taskId: string, attempt: number): string;
